requests==2.31.0
pytz==2024.1
# Modele K en deux etapes (mlb_k_distribution): binomiale tronquee + Poisson.
# Non epingle: la roue doit exister pour le Python du runner (3.11 en CI) comme
# pour celui de la machine de dev.
scipy>=1.11
# Analyse IA (ai_analyst.py). 1.x demande Python >= 3.10 (CI: 3.11). Sans le
# paquet, l'analyse etait sautee en silence meme avec la cle configuree.
anthropic>=1.8,<2
