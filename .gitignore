# Secrets — ODDS_API_KEY, ANTHROPIC_API_KEY. Le depot est public: un .env
# commite expose la cle et il faut la revoquer chez le fournisseur.
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS
.DS_Store

# Cache disque des reponses The Odds API (odds_api.py). Regenerable, et un
# cache commite ferait servir des cotes perimees au prochain run.
.cache/
